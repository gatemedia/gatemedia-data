import modelChanges from 'gatemedia-data/utils/model-changes';

module('modelChanges');

// Replace this with your real tests.
test('it works', function() {
  var result = modelChanges();
  ok(result);
});
