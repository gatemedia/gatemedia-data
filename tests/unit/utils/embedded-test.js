import { module, test } from 'qunit';
import embedded from 'gatemedia-data/utils/embedded';

module('embedded');

// Replace this with your real tests.
test('it works', function (assert) {
  var result = embedded();
  assert.ok(result);
});
