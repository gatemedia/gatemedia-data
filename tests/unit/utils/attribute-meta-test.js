import attributeMeta from 'gatemedia-data/utils/attribute-meta';

module('attributeMeta');

// Replace this with your real tests.
test('it works', function() {
  var result = attributeMeta();
  ok(result);
});
